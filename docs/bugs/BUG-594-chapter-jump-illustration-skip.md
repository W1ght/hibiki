## BUG-594 · 章节翻页初始正确后异步跳过章首插图（TODO-1229 第 5–6 次复诉）
- **报告**：2026-07-07（用户：看板 TODO-1229，「没区别，还是最开始是对的，然后一下子跳到错误的位置」，headless Chrome 真实渲染法证）
- **真实性**：✅ 真 bug。前 4 次修复（图片 late-load 重锚 `__imgReanchorProgress`、spreadReady 门控、惯性跨章冷却/在飞守卫）都打在「输入穿透 / 跳两章」上，与本症状正交，故「没区别」。本症状是**章首插图页被一个异步的 charOffset 重锚越过**：
  - **根因**：`hibiki/lib/src/reader/reader_pagination_scripts.dart` 的 `scrollToCharOffset`（分页 line ~2182）只走文本节点（`createWalker` = SHOW_TEXT）。以插图页开篇的章节里，首个**文本**字符位于插图之后的下一页，故 `scrollToCharOffset(0)` 落到「首个文本页」而非插图页。`restoreToCharOffset(charOffset)`（分页 line ~2058、连续 line ~2830）对 `charOffset==0` 走 `scrollToCharOffset(0)`（**无 page-stable hint**），于是跳过插图。而 `restoreProgress(0)` 走 `scrollToProgressPaged(0)` → `minScroll`，`buildPaginationMetrics.firstContentEdge` 已含前导 block 图（media 循环 line 1977-1985），落点正确=插图页。两条章首语义路径不一致。
  - **触发链（异步第二次锚定）**：前进翻到下一章 → 初始 `restoreProgress(0.0)` 落插图页（**初始正确**）→ `_syncPageSize`（`reader_hibiki_page.dart:2007`）在 content-ready 的 postFrame 里若检出视口宽变（WebView 平台视图/界面缩放 settle 晚一帧，跨章重载后常见）→ `_navigateToChapter(_currentChapter, charOffset: snapshot.charOffset)`（line 2045-2049），而插图页 `getFirstVisibleCharOffset()` 返 **0**（图片无字符）→ 重导以 `restoreToCharOffset(0)` → `scrollToCharOffset(0)` → **跳到正文首页、越过插图**（**过一下跳错**）。`setChromeInsets` 那条重锚带 page-stable hint（±1 页）故不跳；纯文本章/后退(0.99)不涉及本路径，故只在「前进 + 章首插图 + charOffset 重锚」这一组合出现（headless 矩阵法证）。
- **[x] ① 已修复** — `restoreToCharOffset` 把 `charOffset<=0` 归一为「章节绝对起点」：分页走 `scrollToProgressPaged(context, 0)`（=minScroll，含前导插图）、连续走 `scrollToChapterStart()`（滚到顶含前导图），与 `restoreProgress(0)` 章首语义一致；`charOffset>0` 的精确锚行为不变。纯文本章 minScroll==首个文本页，零行为变化；插图章不再被越过。提交见本轮。（`reader_pagination_scripts.dart` 两处 `restoreToCharOffset`。）
- **[x] ② 已加自动化测试** — `hibiki/test/reader/chapter_start_illustration_charoffset_guard_test.dart`（生成 JS 结构守卫：分页/连续 `restoreToCharOffset` 均以 `charOffset <= 0` 判据走 minScroll/scrollToChapterStart，锁死不回退成 `< 0`；且分页 charOffset<=0 分支调 `scrollToProgressPaged(context, 0)`）。真实渲染层用 `tool/reader_pitch_headless/matrix_probe.mjs`（headless Chrome，注入真 shell）复现「image fwd renavChar 跳 1 页」并证修复后归零——CI 跑不到真 WebView，作本地/用户复测法证工具入库。
- **备注**：未动 `window.hoshiReader` 符号；`firstContentEdge` 的 media 纳入（TODO-1179/627）原样保留；不引入 IntersectionObserver（BUG-007）；不污染 TODO-798 userDriven 因果门。charOffset 0 == 0 字已读 == 章首，语义上本应显示章首（插图），故对「停在正文首页且未滚动」的 charOffset 0 亦落章首，属更正确的一致化。真机 4 截图法证（前进翻到含章扉插图的章）由集成 owner / 用户复测。

### 第 6 次复诉（2026-07-08，「没修复。依旧会跳转两次，是因为历史记录恢复的问题吗」）——残留第二跳（连续模式重锚）

- **真实性**：✅ 真 bug，第 5 次修**不完整**。用户猜「历史记录恢复」，实测**不是**——初始开书 / 翻章的历史恢复（`restoreToCharOffset`）第 5 次已修好；残留第二跳来自**连续模式的重锚**。headless 实证（`tool/reader_pitch_headless/chapter_start_reanchor_probe.mjs`，注入真连续 shell + 满页扉页插图 + 正文）：连续模式 `setChromeInsets` / `commitUiScaleReanchor` / `commitStyleReanchor` 三条重锚 **修复前 jumped=1**（从插图页顶 p0 跳到首文本 p1），`restoreToCharOffset`（第 5 次修的那条）jumped=0；分页模式四动作全 0（不复现）。
- **根因**：`hibiki/lib/src/reader/reader_pagination_scripts.dart` **连续版** `scrollToCharOffset`（line ~2790）只走文本节点（`collapsedRangeAtCharOffset`→`createWalker`=SHOW_TEXT），`scrollToCharOffset(0)` 的 range 落在**首个文本字符**（前导插图之后）。连续模式三条重锚——`setChromeInsets`（line ~2866，无 page-stable hint）、`commitUiScaleReanchor`（line ~2907）、`commitStyleReanchor`（line ~2965）——都以 `getFirstVisibleCharOffset()==0`（插图页顶部返 0）**裸调** `scrollToCharOffset(0)` → 滚到首文本字符、越过章首插图。第 5 次只在 `restoreToCharOffset` 入口加了 `<=0` 守卫，拦不到这三条重锚（它们不经 restore 入口）。触发链：前进翻章 → 初始 `restoreProgress(0)` 落插图页顶（**正确**）→ `_onRestoreComplete` 每次翻章都 `_reapplyChromeInsetsAfterFirstLoad`（`chrome.part.dart`）→ `setChromeInsets` → 连续 `scrollToCharOffset(0)` → **跳过插图停到首文本**（残留第二跳）。分页模式的 `setChromeInsets`（line ~2237）带 page-stable hint（`getPagePosition`），插图恰占 1 页时 ±1 兜住不跳（headless 证 jumped=0），故只在**连续模式**复现。
- **[x] ① 已修复**（根因·单一 choke point 归一）— 连续版 `scrollToCharOffset` 入口加 `if (charOffset <= 0) { this.scrollToChapterStart(); return; }`：`<=0 == 章首`（滚到顶含前导图），与 `restoreProgress(0)` 章首语义一致；覆盖 `setChromeInsets`/`commitUiScaleReanchor`/`commitStyleReanchor` **全部**重锚调用者（单点根治，不逐调用者打补丁）；`charOffset>0` 精确锚（含收藏句区间 `endCharOffset`）不变。**分页版故意不加**该守卫：分页所有重锚带 page-stable hint 自行区分「插图页 fvco0（char0 在下一页 → 保留插图页）」vs「首文本页 fvco0（char0 就在本页顶 → 保留首文本页）」，裸归一 `<=0` 会把「停在首文本页」的用户误弹回插图页（headless midChapter 守卫红，故 revert）。headless：修复后连续四动作全 jumped=0、两模式中段守卫 PRESERVED。（`reader_pagination_scripts.dart` 连续 `scrollToCharOffset`。）
- **[x] ② 已加自动化测试** — `hibiki/test/reader/chapter_start_illustration_charoffset_guard_test.dart` 扩两条源码守卫：①连续 `scrollToCharOffset` 入口必有 `if (charOffset <= 0) { this.scrollToChapterStart(); return; }`（锁死覆盖所有重锚）；②回归护栏：分页 `scrollToCharOffset` **不得**裸加 `<=0→章首`（保住 hint 消歧）。真实渲染层复现/验证入库：`tool/reader_pitch_headless/chapter_start_reanchor_probe.mjs`（headless Chrome 注入真分页/连续 shell，覆盖 chromeInsets/renavChar/styleReanchor/uiscaleReanchor + 中段锚保住守卫）+ 配套 shell dump `hibiki/test/reader/reader_headless_shell_dump_test.dart`；CI 跑不到真 WebView，作本地/用户复测法证工具。
- **仍需真机复测**：headless Chrome 已证连续模式三重锚归零、纯文本 / 中段零回归；真机（连续模式 + 章首扉页插图，前进翻到该章）4 截图（初始 / settle 后 / 前进 / 后退）由集成 owner / 用户终验。若用户实为**分页模式**仍复现，则是分页 hint 在「插图跨多页」等边界失效的另一支，需另开 headless 用例定位（当前 headless 分页满页插图不复现）。
