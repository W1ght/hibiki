## BUG-551 · in-app 查词结果区 WebView 下半屏空白(Windows WebView2/WGC 渲染完 idle 无 damage)
- **报告**：2026-07-05（用户：TODO-1152）
- **真实性**：✅ 真 bug。根因 `hibiki/lib/src/pages/implementations/home_dictionary_page.dart`（结果区 `DictionaryPopupWebView` 填满 `Expanded` 全高固定大区域，此前不接 `onRendered`/无渲染后 nudge）+ Windows 渲染桥 `packages/flutter_inappwebview_windows/windows/in_app_webview/in_app_webview.cpp:1770 setSurfaceSize`（`put_Bounds` 撑高后 WebView2 已重排/重栅格，但内容随即 idle、compositor 不再产帧）+ `packages/flutter_inappwebview_windows/windows/custom_platform_view/texture_bridge.cc`（WGC 帧池随 `NotifySurfaceSizeChanged` 重建为全高，却采不到新暴露下半区的帧）→ 下半屏黑。经验佐证：点黑区触发 `custom_platform_view.dart:408 onPointerDown` 把指针事件送进 WebView2 → 产帧自愈。
- **[x] ① 已修复** — 渲染完成（`popupRendered` → `onRendered`）后，对开了 `nudgeSurfaceOnRender` 的宿主补一次「表面重绘 nudge」：Windows 上改 `documentElement` opacity 到 0.9999（肉眼不可辨）跨帧还原，逼 WebView2 产出一帧全页合成 → `OnFrameArrived` → WGC 捕获完整视口。opt-in 开关默认 false（nested 弹窗等自适应宿主原行为不变），仅 in-app 查词结果区 `home_dictionary_page.dart` 开 `nudgeSurfaceOnRender: true`。opacity<1 只建层叠上下文、不为 fixed 后代建包含块（不像 transform），不动图片放大 `.overlay`。文件：`dictionary_popup_webview.dart`（新增 `nudgeSurfaceOnRender` 参数 + `_surfaceRepaintNudgeJs` + `_nudgeSurfaceRepaint()` + popupRendered 处理器条件调用）、`home_dictionary_page.dart`（结果区 WebView 开开关）。提交哈希：见本轮 commit。
- **[x] ② 已加自动化测试** — 源码扫描守卫 `hibiki/test/pages/dict_result_webview_surface_nudge_static_test.dart`：断言 ① widget 暴露 `nudgeSurfaceOnRender`（默认 false）② popupRendered 在开关为真时 Windows-only 调 nudge（opacity 合成）③ home_dictionary_page 结果区 WebView 开 `nudgeSurfaceOnRender: true`。真机像素级下半屏渲满验证走 Windows -Visible（run_windows_itest 观察截图）。
- **备注**：真机 -Visible 验证门：查词页搜词后截图确认下半屏字典内容渲满、无黑块。fork 侧强制全视口重绘为备选（app 侧最小 nudge 优先）。
