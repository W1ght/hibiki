## BUG-651 · 双字幕同显但两条挤在同一位置来回变+样式没按各自轨道
- **报告**：2026-07-08（用户：）· TODO-1341
- **真实性**：✅ 真 bug。根因 `hibiki/lib/src/media/video/video_subtitle_overlay.dart:420-572`（旧 `_buildSubtitleLayer`）。同轨时间重叠、**锚点各异**的 cue（ASSx2 文件 OP/ED/IN 段：顶部 `\an7`/`\an8` 歌词/标题 + 底部 `\an2` 对白同时在屏，实测 `Top_JP(an8)` 与 `Text_JP(an2)`、`ED_EP04_JP(an7)`/`IN_Spring_JP(an7)` 与 `Text_JP(an2)` 时间重叠）被整层塞进**一个** `Column`（旧 `line 442-450`），再用单一「代表」cue `repCue=currentCue`（旧 `line 427-433`）的 `pos`/`anchor` 定位整列（旧 `line 534-571`）。于是两条锚点不同的字幕被裹挟到同一处；`currentCue` 随播放位置在两条间翻转时整列在顶/底来回跳 → 用户报「两个只能同时显示在其中一个的位置·来回变」。样式方面每条 cue 本就用自己的 markup 描边/上色，但位置错乱让「没遵循字幕」的观感成立。
- **[x] ① 已修复** — `git commit cfd9eaed4`。改 `_buildSubtitleLayer`：按各自 markup 的 `\pos`/`\an` 锚点**分组**（新增 `_groupMainCuesByPosition` / `_positionKey`）——同锚点 cue 竖排堆叠进一个字幕盒、不同锚点 cue 各自独立定位（`_positionCueGroup` 返回 `Align`/`Stack`，多组用 `Positioned.fill` 叠放）。交互层（查词点击/模糊/hover）抽成 `_wrapInteractive` 包每组。副字幕层仍强制单组置顶。每条 cue 用自己的 markup（`_buildCueBox`）逐字符描边/上色不变，双轨样式独立。单组/单 cue 退化为单字幕盒，历史几何像素级不变。`hibiki/lib/src/media/video/video_subtitle_overlay.dart:413-661`。
- **[x] ② 已加自动化测试** — `hibiki/test/media/video/video_subtitle_dual_position_test.dart`（4 例）：顶部`\an8`+底部`\an2`重叠→顶在上半/底在下半且明显分离；`currentCue`翻转两条位置稳定不跳（消除「来回变」）；双轨各遵自己主色（`respectAssStyle`）；同锚点重叠仍归一堆叠不回归多位置。全绿，且 8 个既有字幕 overlay 测试文件 69 例零回归。
- **备注**：MarginV（同锚点内不同竖直边距）暂未消费——本 ASSx2 文件为 [JPN] 单语，双轨是「顶部歌词 vs 底部对白」的不同锚点，未触发同锚点内 MarginV 细分；且加 MarginV 会改单 cue 的控制条避让几何（TODO-129/161/238 已调优），风险大、非本次诉求，留后续。真机验收：导入 `C:/Users/wrds/Downloads/QQ/[Nekomoe...Haru no Mai-06...ASSx2][JPN].ass` 播到 OP/ED 段（约 3:09、22:09），确认顶部歌词与底部对白各在其位、不再来回跳。
