## BUG-567 · TLS 默认开后对端封面空白（Image.network 无法钉扎自签证书）
- **报告**：2026-07-06（用户：TODO-1235 / TODO-961 回归）
- **真实性**：✅ 真 bug。根因 `hibiki/lib/src/pages/implementations/home_video_page.dart:1373`（视频封面 `Image.network`）与 `hibiki/lib/src/pages/implementations/reader_history/remote.part.dart:337`（远端书封面 `Image.network`）。TODO-961 TLS 默认开后，host 回填的 `coverUrl` 变成 `https://host/api/library/{videos,books}/<id>/cover`（`hibiki_sync_server.dart:976/1480`）。`Image.network` 走 Flutter 内部 `HttpClient`，没有 `HttpClient.badCertificateCallback`、拿不到 TOFU 钉扎指纹，自签证书 https 握手必失败 → 空封面；而视频流/字幕走 `createPinnedHttpClient`（`webdav_ops.dart:55-68` 经 `_ops.buildRequest`）故能播放，唯独封面漏走钉扎通路。明文 http（TLS 关）下 `Image.network` 恰好能连故此前未暴露。
- **[x] ① 已修复** — 新增封面拉取原语 `RemoteCoverFetcher.fetchRemoteCover`（`hibiki/lib/src/sync/remote_cover_fetcher.dart`），由 `HibikiClientSyncBackend` 实现（`hibiki_client_sync_backend.dart`），复用互联同一条 `_ops.buildRequest` 钉扎通路（https→pinned client 指纹校验；http→裸 client 字节不变；补 Basic auth）。渲染改自定义 `ImageProvider` `RemoteCoverImage`（`hibiki/lib/src/sync/remote_cover_image.dart`，按 coverUrl 键经 ImageCache 去重）。两页封面路径（视频 `home_video_page.dart` / 书 `remote.part.dart`）改走它。删除死掉的 `remote_cover_headers.dart`（唯一用途被取代）。提交：cfe0012bf
- **[x] ② 已加自动化测试** — `hibiki/test/sync/hibiki_client_live_video_test.dart` 新增 group「TODO-1235 pinned cover fetch」：起自签 https `HibikiSyncServer` + 钉扎指纹配置 backend，断言 ①`fetchRemoteCover(https coverUrl)` 拉回真封面字节、②错误指纹时握手失败抛异常、③明文 http 老路径仍能拉回（零破坏）。提交：cfe0012bf
- **备注**：不动 TODO-961 的 TLS/钉扎协议本身；本地封面 `Image.file` 路径不动。
