## BUG-601 · 1295 minTrackCount await破BUG-344 loadToken守卫
- **报告**：2026-07-07（用户：）· TODO-1311
- **真实性**：✅ 真 bug（源码守卫回归，非运行时 UAF）。根因 `hibiki/test/media/video/video_lifecycle_static_test.dart:210`——BUG-344 A 层守卫 `A: selectEmbeddedGraphicTrack captures loadToken before first await` 用 `b.indexOf('await _waitUntilSubtitleTracksReady(player);')`（写死无参调用形态）定位「第一条 await」。TODO-1295（`50e7395ac`）为修副字幕/图形轨「有时不显示」，合法地把这条首 await 改成 `await _waitUntilSubtitleTracksReady(player, minTrackCount: streamIndex + 1)`，`indexOf` 返回 -1、`firstAwaitAt(-1) > tokenAt(602)` 失败 → 守卫红。**生产代码 `video_player_controller.dart:802/805` 的 UAF 不变量始终成立**（loadToken 在首 await 前捕获、每个原生下发后 3 处 `_isCurrentLoad(player, loadToken)` 重校验——同文件其余 A 层守卫全绿佐证）；危害是这条守卫因锚点失配而**恒为 -1、既误红又再也守不住真正的 UAF 重引**（false-negative 化）。`analyze` 不跑测试故集成漏网。
- **[x] ① 已修复**（分支 `todo1311-uaf-guard-fix` / TODO-1311 本轮 commit）—— 守卫锚点从写死整条调用 `...(player);` 收敛到稳定前缀 `await _waitUntilSubtitleTracksReady(player`（不含参数列表），语义不变量（loadToken 必须在首 await 之前捕获）原样保留，仅容忍 TODO-1295 合法加参。与本文件 BUG-528 把锚点收敛到 `await player.open(` 同法。**未改生产代码**：改代码会退回删掉 `minTrackCount` 才能让写死字符串重现，等于回退 TODO-1295 就绪等待功能（禁止）；根因就在守卫锚点，故在守卫层根治。`test/media/video/video_lifecycle_static_test.dart:207-215`。
- **[x] ② 已加自动化测试** —— 同一条源码守卫 `video_lifecycle_static_test.dart:205` 即最强可落地层测试（真 libmpv 无法纯单测），修后 21 项全绿，重新钉死「loadToken 在首 await 前捕获」不变量且对 await 参数变化稳健。整组 `test/media/video/` 1291 项全绿。
- **备注**：selectSecondarySubtitleTrack（TODO-1295 同批改）无对应「before first await」写死锚点守卫，且其 loadToken 同样在首 await 前捕获（源码实测 tokenAt=79 < await=407），无回归。
