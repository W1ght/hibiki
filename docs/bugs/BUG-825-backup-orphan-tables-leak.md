## BUG-825 · 备份导出泄漏合集/标签/书架/搜索历史/删除墓碑等未受类别控制的孤儿表
- **报告**：2026-07-15（用户：只勾「词典 + 音频数据库」导出，备份里却带出了合集 528、搜索历史 60、书架、标签、删除墓碑等个人数据）
- **真实性**：✅ 真 bug。根因 `hibiki/lib/src/sync/backup_service.dart`：`exportBackup` 先 `VACUUM INTO` 整库拷贝，再按类别 strip。裁剪只覆盖 books/videos/progress/statistics/settings/profiles/dictionary + 凭据 + device-local（`_stripCredentials`/`_retainBooks`/`_retainVideos`/`_stripExcludedDataCategories`）。`media_collections`/`media_collection_items`/`shelf_entries`/`book_tags`/`search_history_items`/`book_tombstones`/`statistics_tombstones`/`collection_member_tombstones`/`collection_tag_mappings`/`book_tag_membership_tombstones`/`sync_deletion_tombstones` **不归任何类别管**，随主库无条件导出。实测备份 `media_items=0` 但 `media_collection_items=528` = 悬空引用，佐证合集表脱离裁剪。
- **[x] ① 已修复** — `backup_service.dart` 新增 `_stripOrphanUserDataTables(includeBooks/includeVideos/includeStatistics)`，在 `_retainBooks/_retainVideos` 之后调用。**按类别 gating（非按行解析）**：full 导出什么都不裁（保住 `BackupMergeEngine` 的跨设备合集/标签 union），content-excluding 导出才裁对应内容的行。规则：`search_history_items` 无条件清；epub/video 的成员/书架/`book_tag_membership_tombstones`/`sync_deletion_tombstones` 按 `!includeBooks`/`!includeVideos` 按 `media_type` 清；srt 无类别但 `srt_books` 从不被裁，故 content-excluding 导出按行解析清悬空 srt 成员/书架；被裁空的合集丢弃（保留本就无成员的纯标签合集）；`book_tags` 池排掉不被任何 mapping（含 `collection_tag_mappings`）引用的；`book_tombstones`/`statistics_tombstones`/`collection_member_tombstones` 按对应内容类别清。配对 `hibiki_paired_peers`/`sync_baselines` 早由 BUG-816 裁掉，未动。真实备份 v44 dry-run：孤儿表 23/528/6/5/60/5/7/16 → 全 0，`foreign_key_check` 无违反。
- **[x] ② 已加自动化测试** — `hibiki/test/sync/backup_categories_test.dart` 新增 5 例：full 导出保留删除墓碑（travel to merge）；content-excluding 导出清删除墓碑；合集/书架/标签跟随书导出（保留/裁剪两方向）；悬空 srt 成员 content-excluding 清、full 留；纯标签空合集 full 导出保留。合并侧回归全绿（`backup_merge_collection_tags`/`backup_merge_collection_tombstone`/`backup_merge_import`/`backup_merge_srt_tombstone`）。
- **备注**：`sync_settings_visibility_test`（`sync.content`/`sync.video_files` 可见性）在本分支基线即失败，与本改动无关（未触碰 `sync_settings_schema.dart`），另行处理。
