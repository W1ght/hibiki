## BUG-2721 · KiriKiri：登记了却不画正文的 msgwin 插件让查词采集永远落空
- **报告**：2026-09-27（P1 路线图真机验收发现：PARQUET 2021 ゆずソフトSOUR，Fushi 发现源下载的 files 版）
- **真实性**：✅ 真 bug（真机复现）。accept4：① 文本 PASS（EmbedKrkrZ 线程）、② 语音 PASS（game_resource `man00_002.ogg`）、③ 查词 FAIL（点字无卡）、④ 点击推进。注入侧诊断 `instance.shape: f=0,src=2,c=0,rm=1024,gm=1024`：`kag.renderMsgwinPlugin` 存在，但每句的 `getRender` 都返回 void、`renders` 恒空；全程无 `instance.layers` 行。根因是安装期二选一分派 `native/galgame_hook/hook/adapters/kirikiri_adapter.inc` 安装段「`fushiLookupResolveMsgwinPlugin() !== void` → 只走 msgwin 支」，以及 `fushiLookupSweepLayerRenderers` 开头同一判据的早退：插件「存在」被当成「在画正文」，BUG-2708 的图层渲染器 / classic processCh 兜底对这类构建永远不跑。
- **[x] ① 已修复** — 引擎级判据改为插件自身行为：getRender 包装记录「返回过 void / 返回过有效实例」；在每次采集桥刷新（KAG run 边沿，不依赖已采到几何——句子 surface 刷新依赖几何，会循环）检查「观察到 void、从未产出实例、renders 仍空」即一次性降级到图层渲染器 / classic 采集；之后 getRender 一旦产出有效实例就恢复 msgwin 主导并清 classic 位 0，classic 实例包装按位 0 门控只透传，避免双几何源。另加 `rm=`/`gm=` 成员位图诊断。（本提交）
- **[x] ② 已加自动化测试** — `native/galgame_hook/tests/adapter_structure_test.py::test_kirikiri_inert_msgwin_plugin_falls_back_to_classic`（降级判据、触发点、可逆、图层扫描门、classic 位 0 门控）；原 BUG-2708 守卫仍绿；两份 KiriKiri 源码守卫全量 218 条 OK。
- **备注**：修复后的真机复验**尚未跑**——测试宿主在重启构建时因系统内存告急被 Claude Code 回收。复验命令：宿主起来后 `launchoff …\PARQUET.exe` → 进 PRE STORY 推进到愛花台词 → `thread <EmbedKrkrZ native id>` → `fakeanki` → `accept4 <字形屏幕坐标>`；期望日志出现 `instance.msgwin inert` 与 `instance.layers`，accept4 ③④ 通过。真正走 msgwin 的 2023 柚子社构建（如 天使☆騒々 RE-BOOT!）需另取样本做负向验证。
