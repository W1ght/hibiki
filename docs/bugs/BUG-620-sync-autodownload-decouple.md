## BUG-620 · 同步误把远端独有书自动灌书架(syncAudioBookFiles 触发 TODO-873 自动下书)
- **报告**：2026-07-08（用户：「同步把我没下载的书同步过来了；但之前下载的书籍还是普通书籍，我明明开启了同步有声书文件」）
- **真实性**：✅ 真 bug。根因 `hibiki/lib/src/sync/sync_orchestrator.dart`（修前 `1168-1188` 的 `toPullFullBook` + `1257-1269` 的 Pull B 循环 + `1272-1319` 的 `_pullRemoteOnlyAudiobook`）。`run()` 在互联后端 `syncAudioBookFiles` 开时调 `_syncAudiobooksLive`（`:282`），其中 TODO-873 的 `toPullFullBook` 把「远端有有声书 ∧ 本端**完全没有这本书** ∧ 远端 hasContent」的书当成自动下载对象：`_pullRemoteOnlyAudiobook` 先 `getRemoteBook` 拉 EPUB → `EpubImporter.importFromPath` 灌书架（`booksImported++`）再拉音频。这违反本类自身文档契约（类头 `:126-130` 明说 remote-only EPUB 应等用户手动下载）。用户开的是「同步有声书文件」，却被解读成「自动把远端独有整书拉进书架」。
- **[x] ① 已修复** — 分支 `todo1291-decouple-autodownload`（HEAD）。决策 A 解耦（用户原话「A有书并且开启了同步音频文件才会同步」）：从 `_syncAudiobooksLive` 自动 sweep 路径**摘除** `toPullFullBook` 计算、Pull B 循环与 `_pullRemoteOnlyAudiobook` 方法；`total` 去掉 `toPullFullBook.length`。此后「同步有声书文件」开关下唯一的 pull 语义是 `toPullAudioOnly`——只对**本地已在库**（`localBookKeys.contains(key)`）的书补/拉其音频文件。远端独有书回归手动下载入口（`sync_compare_dialog.dart` / `reader_history/remote.part.dart`，两条手动路径未改动）。开关关时 `run()` 根本不进 `_syncAudiobooksLive`（`:282` 门控），不拉任何音频。代码现与类头 `:126-130` 契约一致。
- **[x] ② 已加自动化测试** — `hibiki/test/sync/sync_orchestrator_live_audio_test.dart` 用例B2（已反转为 TODO-1291 契约）：`远端-only 带有声书 → sweep 后本地不新增 EpubBooks/Audiobooks 行`（`booksImported==0`、`audiobooksImported==0`、`getAllEpubBooks()` 空、`getAllAudiobooks()` 空），及纯文本远端书边界仍不进本地。补音频绿路径由同文件用例B `本地有 EPUB 但缺音频 → 只补音频不重导 EPUB`（`audiobooksImported==1`、`booksImported==0`）守护；关开关不拉音频由用例C `syncAudioBookFiles=false → audiobooksImported==0` 守护。源码接线守卫 `sync_audiobook_download_wiring_guard_test.dart` 的 `toPullAudioOnly`/`localBookKeys` 断言不受影响。
- **备注**：TODO-1291。手动下载远端独有书（含其有声书）走 `remote.part.dart` `_downloadRemoteBook` → `_downloadRemoteAudiobook`，与 orchestrator 自动 sweep 完全独立，本次改动不影响。分项同步开关（统计/词典/内容/有声书文件）Group3 独立持久化，未改语义。
