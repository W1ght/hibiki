## BUG-730 · 剪贴板/全局查词制卡句子字段恒空（未接剪贴板文本）
- **报告**：2026-07-11（用户：剪贴板查词支持制卡，句子=剪贴板内容、单词=制卡词、支持单词音频）
- **真实性**：✅ 真 bug。剪贴板面板 / 瞬态全局查词窗制卡（app 外查词）管线全通——「+」按钮、mine 桥、单词（词条头词）、**单词音频**（`resolveWordAudio`→`_storeRemoteAudio`→`[sound:]`，前提用户配了音频源，注入见 `popup_settings_injection.dart:253-254` 无条件 `window.audioSources`/`needsAudio=true`）、查重、覆写都可用。**唯一缺口=句子**：`overlay_bridge_handlers.dart` 的 `_mineEntry` 只读 `fields['sentence'] ?? ''`，而 JS `buildMinePayload`（`assets/popup/popup.js`）对这两个 app 外表面**从不发 `sentence` 字段** → `{sentence}` 恒空。剪贴板全文早已被捕获进 `ClipboardPanelController._currentSentence`（`clipboard_panel_controller.dart:163`）并渲染成句子横幅，但从没被复制进制卡上下文；`clipboard_panel_controller.dart:299-301` 注释声称「兼作制卡 sentence 字段」实为空头支票（与 spec `docs/specs/2026-07-10-clipboard-panel-lookup-design.md:30-31` 的意图不符）。
- **[x] ① 已修复** — Dart 宿主侧注入（零改 popup.js，避开浏览器扩展 3-way parity + content.css 重生成 + 扩展制卡双注入风险）。`maybeHandleOverlayDeferredBridge` 加 `sentenceContext`，新纯函数 `resolveMineSentence(fields, sentenceContext)`（JS 的 `fields['sentence']` 非空优先，否则回落 context），`_handleMineBridge`/`_mineEntry`/`_recordMinedStats` + `_handleUpdateBridge`/`_updateEntry` 全用它；两调用点各传自己捕获的句子：`clipboard_panel_controller.dart`（剪贴板全文）、`global_lookup_controller.dart`（UIA 前台句）。制卡历史行同句不再空。单词音频既有能力已足，无需改。文件：`hibiki/lib/src/lookup/overlay_bridge_handlers.dart`、`hibiki/lib/src/lookup/clipboard_panel_controller.dart`、`hibiki/lib/src/lookup/global_lookup_controller.dart`。提交：cee055c9a
- **[x] ② 已加自动化测试** — ①`hibiki/test/lookup/overlay_mine_sentence_context_test.dart`：纯函数 `resolveMineSentence` 4 例 + 接线源码守卫。②`hibiki/test/lookup/overlay_mine_sentence_behavior_test.dart`（**行为级运行时证据**，非源码扫描）：构造真实 `AppModel`+捕获型 Anki repo，把真实 JS `mineEntry` 消息喂进生产 handler（`maybeHandleOverlayDeferredBridge→_mineEntry→resolveMineSentence→repo.mineEntry`），断言到达 `AnkiMiningContext.sentence` 的正是剪贴板整句（过去恒空）；含 JS 句优先 / 双空不崩。无需词典/覆盖窗/在线 Anki。3 例全绿。仅剩「可见窗 + 真 Anki 卡目视 `{sentence}`/`{audio}`」需真机（隔离 harness 无用户词典/Anki 配置，且不应往用户真库建卡）。提交：cee055c9a / 4b41fea12
- **备注**：待真机 gate——剪贴板复制一句 → 面板点词 →「+」制卡 → 验 Anki 卡 `{sentence}` = 剪贴板全文（命中词加粗）、`{audio}` = 单词发音（需先配音频源）。修复层在共享 `overlay_bridge_handlers.dart`，瞬态窗与面板同修。
